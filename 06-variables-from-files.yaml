- name: Variables from different file
  hosts: web
  vars_files:
  - variables.yaml
  tasks:
  - name: Variables getting from different file
    ansible.builtin.debug:
      msg: "Hello this is {{Name}} learing {{Course}} and duration is {{Duration}}"